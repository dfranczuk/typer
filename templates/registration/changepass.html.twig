{% extends 'base.html.twig' %}

{% block title %}Change password{% endblock %}

{% block body %}
    <h1>Change password {{ user.username }}</h1>

    {{ include('registration/_form.html.twig', {'button_label': 'Change password'}) }}

    <a href="{{ path('user_index') }}">back to list</a>


{% endblock %}
