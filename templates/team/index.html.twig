{% extends 'base.html.twig' %}

{% block title %}Team index{% endblock %}
{% block javascripts %}
    {{ parent() }}

    <script>


      /*  $("#btn_check").click(function(){
            if($('#rep_check').prop('checked', true))
                $('#type')
            else if($('#cl_check').prop('checked', true))
            }


        )*/
    </script>
{% endblock %}
{% block body %}
    <h1>Team index</h1>
    <label id="rep_check"><input type="checkbox" checked>Representation</label>
    <label id="cl_check"><input type="checkbox" checked>Club</label>
    <button class="btn" id="btn_check">{{ button_label|default('Search') }}</button>
    <table class="table">
        <thead>
            <tr>

                <th>Id</th>
                <th>Name</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for team in teams %}
            <tr>
                <td>{{ team.id }}</td>
                <td>{{ team.name }}</td>
                <td id="type">{{ team.type }}</td>
                <td>
                    <a href="{{ path('team_show', {'id': team.id}) }}">show</a>
                    <a href="{{ path('team_edit', {'id': team.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('team_new') }}">Create new</a>
{% endblock %}