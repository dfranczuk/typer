{% extends 'base.html.twig' %}

{% block title %}TypeofWeight{% endblock %}

{% block body %}
    <h1>TypeofWeight</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeof_weight.id }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ typeof_weight.Weight }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typeof_weight_index') }}">back to list</a>

    <a href="{{ path('typeof_weight_edit', {'id': typeof_weight.id}) }}">edit</a>

    {{ include('typeof_weight/_delete_form.html.twig') }}
{% endblock %}