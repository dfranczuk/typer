{% extends 'base.html.twig' %}

{% block title %}{{ 'Nowy użytkownik' | trans }}{% endblock %}

{% block body %}
    <h1>{{ 'user_table.new' | trans }}</h1>

    {{ include('user/_form.html.twig') }}

    <a href="{{ path('user_index') }}">{{ 'user_table.back' | trans }}</a>
{% endblock %}
