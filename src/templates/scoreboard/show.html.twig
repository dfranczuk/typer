{% extends 'base.html.twig' %}

{% block title %}Scoreboard{% endblock %}

{% block body %}
    <h1>Scoreboard</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scoreboard.id }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ scoreboard.points }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('scoreboard_index') }}">back to list</a>

    <a href="{{ path('scoreboard_edit', {'id': scoreboard.id}) }}">edit</a>

    {{ include('scoreboard/_delete_form.html.twig') }}
{% endblock %}